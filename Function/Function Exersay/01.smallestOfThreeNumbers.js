// Write a function that receives three integers and prints the smallest number. Use an appropriate name for the
// function.

function smallestOfThreeNumbers(firstNum, secondNum, thirdNum) {
    let smallest = Math.min(firstNum, secondNum, thirdNum)


    console.log(smallest);
}

smallestOfThreeNumbers(2,
    5,
    3)

